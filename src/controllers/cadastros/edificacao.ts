import { Response } from "express";
import { AuthenticatedUserRequest } from "../../middleware";
import {
  edificacaoDeleteService,
  edificacaoGetAllService,
  edificacaoGetService,
  edificacaoPostService,
  edificacaoPutService,
} from "../../services/cadastros/edificacao";

export const dadosCadastroEdificacao = {
  post: async (req: AuthenticatedUserRequest, res: Response) => {
    try {
      const { descricao } = req.body;
      const { empresaId } = req.user!;

      const data = await edificacaoPostService(empresaId.toString(), descricao);

      res.send(data);
    } catch (err) {
      if (err instanceof Error) {
        return res.status(400).json({ message: err.message });
      }
    }
  },
  getAll: async (req: AuthenticatedUserRequest, res: Response) => {
    try {
      const { empresaId } = req.user!;
      const data = await edificacaoGetAllService(empresaId.toString());
      res.send(data);
    } catch (err) {
      if (err instanceof Error) {
        return res.status(400).json({ message: err.message });
      }
    }
  },

  get: async (req: AuthenticatedUserRequest, res: Response) => {
    try {
      const { empresaId } = req.user!;
      const { idedificacao } = req.params;
      const data = await edificacaoGetService(
        empresaId.toString(),
        idedificacao
      );
      res.send(data);
    } catch (err) {
      if (err instanceof Error) {
        return res.status(400).json({ message: err.message });
      }
    }
  },

  put: async (req: AuthenticatedUserRequest, res: Response) => {
    try {
      const { empresaId } = req.user!;
      const { idedificacao } = req.params;
      const { descricao } = req.body;
      const data = await edificacaoPutService(
        empresaId.toString(),
        descricao,
        idedificacao
      );
      res.send(data);
    } catch (err) {
      if (err instanceof Error) {
        return res.status(400).json({ message: err.message });
      }
    }
  },

  delete: async (req: AuthenticatedUserRequest, res: Response) => {
    try {
      const { empresaId } = req.user!;
      const { idedificacao } = req.params;
      const data = await edificacaoDeleteService(
        empresaId.toString(),
        idedificacao
      );
      res.status(204).json(data);
    } catch (err) {
      if (err instanceof Error) {
        return res.status(400).json({ message: err.message });
      }
    }
  },
};
